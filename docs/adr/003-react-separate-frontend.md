# ADR 003: Separate React Frontend

## Status
Accepted

## Decision
Move UI to `frontend/` React SPA; backend is API-only with CORS/nginx proxy.

## Consequences
Production-grade separation, independent deploy/build, better UX tooling.
